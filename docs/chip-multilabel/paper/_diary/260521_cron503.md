# cron #503 — pair_g4_v2 final-ckpt overturns Addendum 5

## Trigger
- pair_g4_v2 final epoch (ep8) eval landed: bit_F1 0.9915 / Total FAR 0.68%.
- best-ckpt eval previously reported: bit_F1 0.8944 / Total FAR 46.82%.
- Delta on same run between val_acc-best ckpt and final ckpt is extreme
  (bit_F1 +0.0971 absolute, Total FAR -46.14 pp).

## Implication for §5.52 Addendum 5
- Addendum 5 had concluded "Double-A reinforcement, pair g harm" from the
  best-ckpt comparison (pair_g4_v2 best 0.8944 vs single best 0.95+).
- Final-ckpt numbers reverse the conclusion: pair_g4_v2 final 0.9915 is at
  parity (or above) single-defect runs.
- Hypothesis: val_acc plateau during late epochs makes the early ckpt look
  "best" while the final ckpt has actually learned the multi-label combo
  structure better. Ckpt selection criterion is misrepresenting the model.

## Action policy (260521, follows paper composition rule)
- Do NOT edit §5.52 / §5.54 / Abstract yet — single run, not yet replicated.
- Wait for nopair_g4_v2 final eval to confirm whether the gap is recipe-wide
  or pair-specific.
- If nopair_g4_v2 final also shows large best→final delta, escalate to
  §5.54 new section "Best-vs-Final ckpt under val_acc plateau:
  selection bias in multi-label evaluation".
- If only pair_g4_v2 shows it, narrower edit: footnote inside §5.52
  qualifying Addendum 5 ("conclusion conditional on best-ckpt selection").

## WHY (design decision)
- Single-run reversal can be noise (seed, plateau jitter). Replication on
  the sister recipe (nopair_g4_v2) is the minimum bar before editing
  paper-grade narrative. Avoids whiplash retraction cycle.

## Reference points
- §5.52 Addendum 5 narrative: docs/chip-multilabel/paper/05_experiments.md
- pair_g4_v2 best ckpt eval (prior): 0.8944 / 46.82% (logged in cron 444 era)
- pair_g4_v2 final ckpt eval (new, cron #503): 0.9915 / 0.68%

## Negative-result value
- Even if nopair_g4_v2 final flattens this, the discovery that one ckpt
  selection swap moves Total FAR by 46 pp is itself paper-worthy as a
  methodology caveat — current eval protocol is fragile to selection rule.

## Pending
- nopair_g4_v2 final-ckpt eval (next cron).
- Confirm: best vs final ckpt delta is recipe-general OR pair-only.
