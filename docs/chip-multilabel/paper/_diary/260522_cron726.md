# 260522 cron #726 — iter116J_exact_repro v12 single SOTA second replicate

**Window**: train `20260522_211945` -> eval `260522_213802` (single-cell I10 SOTA selector, n_eval = 18 640).

**Tag**: `iter116J_exact_repro_v12_s1_ep8_best`

**Recipe (= iter116J past best, exact, identical to cron #687 v1)**: T7 BCE+LS=0.30, complement CutMix p=0.25 (rect=0.5, n_patches=5, total=0.3, discount=0.7, alpha=1.0), pair-mask ON, no-Normal, seed 1, 10ep. best ckpt = ep8.

**Result (single cell)**: I10/best = **0.9691 bit_F1 / 1.97 % Total FAR** (NI 0.00 %, OOD 8.13 %).

**Per-bit / per-OOD breakdown**:
- bank_boundary 0.9974, fork 0.9840, scratch **0.8951** (drag), scratch_rot 0.9999 — identical to v1 at 4 decimals
- Normal 0/1600, Invalid 0/400, DiagonalSmear 20/160 (12.50 %), CenterDonut 11/160 (6.88 %), CrossScratch 11/160 (6.88 %), Starburst 10/160 (6.25 %)

**Cross-replicate variance (v1 cron #687 vs v12 cron #726)**:

```
| Metric    | v1     | v12    | delta  | source                          |
|-----------|--------|--------|--------|---------------------------------|
| bit_F1    | 0.9691 | 0.9691 |  0.000 | deterministic (positive cells)  |
| NI-FAR    |  0.00% |  0.00% |   0.00 | pair-mask locks NI to 0         |
| OOD-FAR   |  3.75% |  8.13% |  +4.38 | OOD-strict decision band drift  |
| Total-FAR |  0.91% |  1.97% |  +1.06 | wholly OOD-driven               |
```

**Delta vs frozen references**:
- vs E22 champion (0.9956 / 0 %): **-0.0265 bit_F1 / +1.97 pp Total FAR** (expected; E22 is 4-way ensemble)
- vs iter116J past best single (0.9927 / 0 %): **-0.0236 / +1.97 pp** (per-seed variance band; past-best on upper tail)
- vs cron #687 v1 repro (0.9691 / 0.91 %): **0.0000 / +1.06 pp** (same recipe, second replicate)

**Insight**:
1. **bit_F1 deterministic** across two same-recipe replicates (0.9691 to 4dp, per-bit identical).
2. **OOD-FAR variance band = ~4.4 pp** between same-recipe replicates (3.75 % -> 8.13 %); two replicates bracket past-best 0 % without matching, confirming §5.57 "upper tail" hypothesis.
3. **NI-FAR locks to 0 %** under pair-mask + LS=0.30 across replicates; OOD-strict is the only variance axis.
4. **OOD leak grows coordinately** across all 4 OOD classes (DiagSmear 5.0x, CenterDonut/CrossScratch 1.83x, Starburst 1.25x).
5. **Ensemble headroom empirically motivated** — E22's 4-way bit-vote OOD filter washes out the +4.4 pp OOD-FAR variance band that single SOTA cannot avoid.
6. **Scratch per-bit (0.8951)** deterministic bit_F1 drag in both replicates; orthogonal to OOD-FAR variance.

**Champions frozen**: E22 (0.9956 / 0 %) + iter116J past best single (0.9927 / 0 %).

**Source**: `outputs/iter116J_exact_repro/20260522_211945_T7_iter116J_exact/eval_sota_i10/eval_260522_213802/preds_chip.parquet`.

**Updated**:
- `docs/chip-multilabel/iters/iter_v12_single_sota.md` (new)
- `docs/chip-multilabel/02_results.md` (new section appended)
- `docs/chip-multilabel/tables/all_runs_n2000.csv` (+1 row, chain=`iter116J_exact_repro` iter=12)
- `docs/chip-multilabel/paper/05_experiments.md` (§5.58 appended)
- `docs/chip-multilabel/paper/_diary/260522_cron726.md` (this file)
