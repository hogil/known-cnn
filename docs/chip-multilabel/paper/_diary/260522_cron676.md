# 260522 cron #676 — FCM-PM val_margin chain (10 trains, 18 cells) recorded

§5.55 appended (paper) and full 18-cell table appended to 02_results.md.

10 FCM-PM single-model retrains (5 hparam cond × pair/nopair fork), evaluated under `val_margin` at both `best_model.pth` and `final_model.pth` ckpts, I10 selector only.

Headlines:
- Single-model new high: `g3_cls07_ls30_nopair` = **0.9953** bit_F1 (best == final), but Total FAR 0.12 % (OOD-FAR 0.37 %, Invalid leak). +0.0026 vs iter116J, -0.0003 vs E22.
- Closest to iter116J cleanness: `g4_cls05_ls30_nopair_best/I10` = 0.9923 / 0.09 % (-0.0004 / +0.09 pp).
- pair wins FAR in 5/5 cond (every `pair_final` Total FAR <= 0.01 %); nopair wins bit_F1 in 4/5 cond.
- Largest `best->final` headroom = `g4_cls05_ls30_pair` (FAR 0.30 -> 0.006, +0.0098 bit_F1). Replicates §5.54.
- Six clean-FAR (<= 0.02 %) cells with bit_F1 >= 0.96 over diverse (g2, g3, g4) x (pair, nopair) -> ensemble candidate pool.

Champions frozen: E22 (0.9956 / 0 %) ensemble, iter116J (0.9927 / 0 %) single-model.

18 rows appended to tables/all_runs_n2000.csv (chain=fcm_margin).

Source: 10 x `outputs/fcm_margin_*/20260521_*/eval_n2000_margin_{best,final}/eval_*/bit_far_metrics.json`.
