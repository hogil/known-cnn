# 2026-05-24 cron #960 — Reproducibility audit closure

Trigger: cron #960 10-min auto re-trigger; I13 v5 OOD eval completed.

New result:
- **I13 v5 OOD**: bit_F1 = 0.9709, Total FAR = 0.0000.
- Frozen reference: iter116J `T0__I13` macro_f1 = 0.9709
  (260516 `eval_n2000_REPRO`).
- Match: exact.

Companion (cron #959, already logged):
- **I10 v5 OOD**: bit_F1 = 0.9919 vs frozen 0.9918 (Δ = +0.0001,
  within float noise).

Audit status:
- Both variants (I10, I13) under v5 OOD generator now reproduce
  their frozen records to within rounding.
- Recipe reproducibility = complete when (variant ID, generator
  version) is pinned. The two-axis under-specification (decision
  rule + generator version) identified in §7.14 is the sole
  driver of external-reader drift.

Action:
- Appended §7.14.1 "Audit closure" to `07_discussion.md`.
- Marked audit complete.
- Flagged `02_results.md` transcription artefact: claimed
  I13 = 0.9927 contradicts both frozen (0.9709) and reproduced
  (0.9709) numbers. Likely an I10 row mis-labelled as I13.
  Not auto-edited — flagged for separate corrigendum with
  human verification of champion-row provenance.

Section closed.
