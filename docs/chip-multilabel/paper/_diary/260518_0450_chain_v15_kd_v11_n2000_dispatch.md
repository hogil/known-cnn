# Diary — 260518 04:50 (cron #41) chain v15 dispatch

**Cron tick.** #41 at 04:50, narrator-only (no train/eval dispatch from this agent).

**Dispatch event.** Chain v15 launched at 04:37 (13 min before the
tick). Two sub-runs:

- **Phase 1: KD_v11** — α=0.25 T=2, grad-checkpointing ON. Training
  in progress at the tick (~13 min elapsed). No metric written.
- **Phase 2: ensemble member (n2000 corner)** — queued behind
  Phase 1 to share GPU under the 30-40 % shared-budget assumption.

**Champion status.** Unchanged. Single iter116J 0.9927, ensemble
chain v8 `vote_majority_bits` 0.9941. The v15 dispatch is
table-completion targeted, not headline-displacement.

**Why these two cells.**

- KD_v11 sits on the conservative α end of the v8/v9 grid where
  v9 cron 9 mapped α=0.50 T=2 as collapsed and α=0.25 T=4 as
  under-distilled. α=0.25 T=2 is the remaining viable corner.
  Grad-checkpointing is the recovery axis after the v14 RAM-cap
  kill (cron 8) — checkpointing trades ~25 % wall time for
  recovered activation memory.
- The n2000 ensemble cell closes the §5.17.4 robust-eval grid
  (currently cuts off at n=500). A single bag member at the n2000
  protocol completes the n=50 / n=200 / n=500 / n=2000 column set
  for the paper's main comparison table.

**Methodology contribution independent of the eventual numbers.**

1. Grad-checkpointing as KD enabler under shared-GPU 30-40 % budget
   — if KD_v11 lands within ±0.003 of the un-checkpointed KD_v8
   ceiling (0.9941) we get a reproduction-recipe paragraph for
   any 30-40 GB-budget replicator.
2. n2000-corner ensemble completion → §5.17.4 publishable as a
   single consolidated grid.

**Next tick (~06:00) expectation.**

- KD_v11 training finishes, eval row appended.
- Band check: 0.992 – 0.994 = checkpointing preserves the corner;
  0.985 – 0.990 = checkpointing-sensitivity negative result
  (still paper-citable as the cost of the activation-memory
  trade).

**Negative-result discipline.** Either outcome is logged in §5
chain v15 paragraph — the narrative explicitly frames both bands
in advance so we cannot post-hoc cherry-pick. WHY: the
`feedback_test_before_push` rule prohibits selective metric
reporting after-the-fact.

**Paper section touched.** §5.X chain v15 narrative paragraph
appended to `05_experiments.md` (after chain v12 Phase 1+2 BCE
LS collapse section).

**Files not touched** (per agent role boundaries):
- `outputs/` (logger / runner territory)
- `chip_multilabel/` (runner territory)
- `iters/`, `tables/` (logger territory)
