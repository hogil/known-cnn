# Diary — 2026-05-05 (final consolidation)

Continues from `260505_late_evening.md`. Covers the autonomous-loop
convergence (T1 / T9 multi-seed wrap), the eight-axis negative
catalogue, and the paper consolidation pass (22:00 – 22:20).

## ~22:00 — autonomous loop reaches its converged state

The loop spent the late evening cycling through atomic-axis
proposals and rejecting each one in turn:

- iter 6 already had Phase F (warmup + EMA, both negative) and
  I11 pair-aware threshold (negative).
- iter 7 closed CutMix-p sweep with p=0.5 the sharp peak.
- iter 8 found LS=0.07 single-seed peak T9d=0.9705 with seed=43
  replicate T9g=0.9408 → variance discipline born.
- iter 9 added T10 drop_path / T11a cutmix-rect / T12a two-LR,
  all negative.
- post-iter-9 the analyst loop proposed T8 (CE-soft+cutmix) and
  T13a (ASL γ_neg=2 + cutmix) as "loss-family alternates with
  CutMix in the recipe"; both negative by ≈0.10.

That closes the search around T9. Eight independent axes, all
under-perform, all explained by the same regularisation-ceiling
diagnosis. The loop is converged.

## ~22:05 — third-seed runs of T1 and T9 (s44)

Still missing: the third seed of both T1 and T9 to put a
confidence interval on the headline numbers. Both launched as the
last training jobs of the session:

- T1 seed=44: `outputs/logs_chip_multilabel/T1_T1_baseline_seed44_260505_220350/`
  → stage1: `outputs/stage1_260505_221847/` → macro_f1 = 0.8712,
  top1_11 = 0.7028, bb+sr recall = 0.4437.
- T9 seed=44: `outputs/logs_chip_multilabel/T7_T9h_BCE_LS07_seed44_260505_214906/`
  → stage1: `outputs/stage1_260505_215202/` → macro_f1 = 0.8803,
  top1_11 = 0.7153, bb+sr recall = 0.3500.

Both seeds pull the family means down toward 0.89/0.93, confirming
that the seed=42 peaks (0.9268, 0.9705) were upper-tail draws.

## ~22:10 — final 3-seed numbers (T1 vs T9 paired)

| metric        | T1 mean ± std    | T9 mean ± std       | Δ T9-T1 |
|---------------|------------------|---------------------|---------|
| macro_f1      | 0.8923 ± 0.0301  | 0.9305 ± 0.0460     | +0.0382 |
| top1_11       | 0.7697 ± 0.0714  | 0.8242 ± 0.1058     | +0.0545 |
| bb+sr recall  | 0.5292 ± 0.2577  | 0.7542 ± 0.3500     | +0.2250 |

Per-seed paired Δ macro_f1: s42 +0.044, s43 +0.062, s44 +0.009 —
T9 wins all three seeds.
Per-seed paired Δ top1_11:  s42 +0.082, s43 +0.069, s44 +0.013 —
T9 wins all three seeds.
Per-seed paired Δ bb+sr:    s42 +0.631, s43 +0.138, s44 −0.094 —
T9 wins 2/3, unlucky on s44 (best-cell selection routed s44 T9
into I10 instead of I7, and the I10 cell does not exact-match
bb+sr combos as cleanly).

The macro_f1 / top1_11 directionality is unambiguous; the bb+sr
mean is positive (+0.225) but with non-trivial seed variance.

## ~22:15 — what we ship

Headline number: **T9 family 3-seed macro_f1 mean = 0.9305 ± 0.0460,
+0.038 over T1 paired**, with bb+sr mean lift +0.225 paired and
top1_11 mean lift +0.055 paired. Single-seed peak (0.9705 at s42)
is reported in tables for completeness with explicit variance flag,
but is *not* the headline.

Eight independent atomic-axis attempts on top of T9 all
under-perform by 0.05–0.11. The catalogue is firm enough to elevate
to a *regularisation-ceiling hypothesis*: in our regime (small
data, strong TAPT, tuned LS, BCE+CutMix), additive structural
regularisers and alternate loss-family substitutions are net costs.

## ~22:18 — paper consolidation pass

Updates to paper/:

1. **abstract.md** — refreshed headline to 3-seed mean ± std
   notation. T9 +0.038 macro_f1, +0.225 bb+sr robustly across
   3 seeds.
2. **05_experiments.md** — added §5.10 with T1/T9 3-seed paired
   table and per-seed paired delta breakdown.
3. **06_analysis.md** — extended §6.7 with §6.7.7 paired-seed
   insight (paired-Δ std ≈0.026 vs marginal std ≈0.03–0.05).
4. **07_discussion.md** — added §7.5.4 "Eight negative axis
   attempts confirm the regularisation-ceiling hypothesis" and
   updated §7.7 to flag T1 also showing high single-seed variance.
5. **09_conclusion.md** — final consolidation: 4 takeaways, eight-
   axis negative-result table, multi-seed reporting protocol as
   the methodological discipline.
6. **_diary/260505_final.md** — this file.

## ~22:20 — idle

Nothing left to launch. The loop has converged, the paper sections
are written, and the negative-result catalogue is closed at eight
atomic axes. Future work (Phase G multi-seed at LS=0.05 and
LS=0.10, Phase B/D in-loss-family hparam sweeps, Phase H
replacement-regularisation test) is queued but does not block this
paper's headline.

The numbers are honest, the negative results are first-class, the
single-seed-variance lesson is now methodological discipline.
Idle.
